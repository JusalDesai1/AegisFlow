'use client';\n\nimport { colors, shadows, spacing, typography } from '@/styles/tactical-neomorphism';\nimport useDisasterStore from '@/store/disasterStore';\n\nexport function MetricsCards() {\n  const metrics = useDisasterStore((state) => state.metrics);\n\n  const metricCards = [\n    { label: 'Active Incidents', value: metrics.activeIncidents, icon: '🚨', color: colors.critical },\n    { label: 'Affected Population', value: `${(metrics.affectedPopulation / 1000).toFixed(1)}k`, icon: '👥', color: colors.warning },\n    { label: 'Resources Deployed', value: metrics.resourcesDeployed, icon: '📦', color: colors.success },\n    { label: 'Avg Response Time', value: `${metrics.responseTime}m`, icon: '⚡', color: colors.info },\n    { label: 'Active Shelters', value: metrics.sheltersActive, icon: '🏢', color: colors.accent },\n  ];\n\n  return (\n    <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-4\">\n      {metricCards.map((card, idx) => (\n        <div\n          key={idx}\n          style={{\n            background: colors.card,\n            border: `1px solid ${colors.border}`,\n            borderRadius: '12px',\n            padding: spacing.lg,\n            boxShadow: shadows.subtleDark,\n          }}\n          className=\"hover:shadow-lg hover:scale-105 transition\"\n        >\n          <div style={{ fontSize: '2rem', marginBottom: spacing.md }}>{card.icon}</div>\n          <div style={{ ...typography.label, color: colors.textTertiary, marginBottom: spacing.sm }}>\n            {card.label}\n          </div>\n          <div style={{ ...typography.h3, color: card.color }}>{card.value}</div>\n        </div>\n      ))}\n    </div>\n  );\n}\n